To push to heroku:
git add . && git commit -m "YOUR COMMIT MESSAGE" && git push heroku master

If there are changes made to the database need to run the following commands:

heroku run python manage.py db upgrade --app pythonserver-neatlabs

To run the code locally:
If there are any changes to the db run: python manage.py db migrate, python manage.py db upgrade

Then run: python manage.py db runserver 

To submit data go to 
url will be either http://127.0.0.1:5000 or https://pythonserver-neatlabs.herokuapp.com/

<url>/addwakesleeptime
-> User can update their waking and sleeping time

<url>/getallusers
-> Can retrieve all users by their unique identifiers and emails
